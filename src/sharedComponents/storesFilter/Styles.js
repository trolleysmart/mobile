// @flow

import { StyleSheet } from 'react-native';

export default StyleSheet.create({
  row: {
    padding: 10,
  },
  description: {
    fontSize: 20,
    alignItems: 'center',
    justifyContent: 'center',
  },
});
