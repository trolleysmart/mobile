// @flow

import {
  StyleSheet,
} from 'react-native';

export default StyleSheet.create({
  container: {
    padding: 15,
    paddingBottom: 40,
    marginBottom: 20,
  },
  title: {
    fontWeight: 'bold',
  },
  button: {
    backgroundColor: '#3DC62A',
  },
});
