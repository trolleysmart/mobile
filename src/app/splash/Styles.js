// @flow

import { StyleSheet } from 'react-native';

export default StyleSheet.create({
  grid: {
    alignItems: 'center',
  },
  activityIndicator: {
    alignItems: 'center',
    justifyContent: 'center',
    padding: 8,
  },
});
